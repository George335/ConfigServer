server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/George335/ConfigServer.git
          searchPaths: config-data
          username: George335
          password: perseo335
  security:
    user:
      name: root
      password: s3cr3t  
